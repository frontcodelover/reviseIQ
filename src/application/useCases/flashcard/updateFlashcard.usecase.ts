// import { FlashcardRepository } from '@/domain/repositories/FlashcardRepository';
// import { FlashcardUpdate } from '@/domain/entities/Flashcard';

// export class UpdateFlashcardUseCase {
//   constructor(private flashcardRepository: FlashcardRepository) {}
//   async execute(
//     flashcardId: string,
//     deckId: string,
//     userId: string,
//     updates: FlashcardUpdate
//   ): Promise<void> {
//     await this.flashcardRepository.updateFlashcard(flashcardId, deckId, userId, updates);
//   }
// }
